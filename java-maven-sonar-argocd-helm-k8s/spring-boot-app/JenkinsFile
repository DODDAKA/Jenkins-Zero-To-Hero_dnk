pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh 'echo passed'
                // Uncomment the following line if you want to checkout from a specific Git repository
                // git branch: 'main', url: 'https://github.com/iam-veeramalla/Jenkins-Zero-To-Hero.git'
            }
        }
    }
}
